# Channel Matrix

| Channel | Contact purpose | Install object | Intended account / owner | Visibility | Authority | Current state | Verification path |
|---|---|---|---|---|---|---|---|
| GitHub repository and release | One public source, release, support, and install locus for the Augment | Approved repository tree, tag, release assets, checksums | `Stunspot/impactful-tom`; Sam Walker / Collaborative Dynamics | Public after product, verification, documentation, and package gates | Public GitHub publication and retention of the Impactful Tom name are authorized by Sam; no third-party endorsement or affiliation is claimed | No remote repository, tag, release, or public URL is observed | Read back owner, visibility, default branch, tag, release metadata/assets, then clone and hash main and tag |
| Codex repository marketplace | Native Codex plugin discovery/install route | `.agents/plugins/marketplace.json` and `plugins/impactful-tom` | Same GitHub repository | Inherits repository visibility | Publication authorization retained; OpenAI Directory submission is out of scope | Local marketplace configured; candidate installed and enabled with cache parity on the current host; public install unobserved | Follow the public repository install route in a fresh Codex session, identify installed bytes, invoke `impactful-tom`, and run a representative case |
| Claude Code / generic skill release asset | Portable fallback and host-specific installation route | `dist/claude-code/impactful-tom` in a verified archive | Same GitHub release | Public with the GitHub release | Same public-release authorization; no separate directory submission | Static package tree is present; no live Claude host behavior is observed | Extract the released archive in a clean authorized host, invoke the skill, and complete one representative founder request |
| OpenAI Plugins Directory | Not requested | None | N/A | N/A | Not requested | Deliberately out of scope | Record as not submitted; do not infer directory availability from GitHub publication |

## Channel rule

The local Codex marketplace and a pre-delta plugin candidate were configured on the current workstation; its later task-catalog presence and representative first-success behavior were observed. The final instruction body is byte-identical, while two routing descriptions and four legal/notice files changed. Public-route installation of the exact final package remains unexecuted. The product name and license decisions are settled. Documentation gates pass; publication remains gated on packaging, final release review, and public readback.
